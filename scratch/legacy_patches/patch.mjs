import fs from 'fs'; let c = fs.readFileSync('src/core/NodeRuntime.ts', 'utf8'); c = c.replace('const activeRun = this.activeRoomRuns.get(roomId);', 'const runId = \\\${roomId}:\\\;\\n    const activeRun = this.activeRoomRuns.get(runId);'); c = c.replace('this.roomStopRequests.add(roomId);', 'this.roomStopRequests.add(runId);'); c = c.replace('const run = this.startRoomRun(roomId, () => this.runRoomSession(roomId, runtimeRole, taskHint, adapter, seededState, autonomous));', 'const run = this.startRoomRun(runId, () => this.runRoomSession(roomId, runtimeRole, taskHint, adapter, seededState, autonomous));'); fs.writeFileSync('src/core/NodeRuntime.ts', c);
